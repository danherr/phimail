var React = require('react');


var SideBar = React.createClass({
  render: function () {
    return (
      <section className="sidebar">
        SideBar
      </section>
    );
  }
});

module.exports = SideBar;
